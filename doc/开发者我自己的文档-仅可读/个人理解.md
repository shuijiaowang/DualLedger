双记账系统（DualLedger）。
同时支持两种记账视角，消费记录和消耗记录。
现金流视图（普通记账）：钱哪天付出去/收进来，和常规记账一样。
权责视图（消费记账）：哪天真正消耗了价值，（平摊30天、按件数、按使用天数动态摊销）

先写后端和web前端，稳定后再复制到uniapp写app和小程序，会轻松很多。
数据库使用mysql。
前端使用Naive UI，


基础就是收入/支出，但我不清楚如转账/借钱/押金是怎样的？这部分好像不需要权责视图？
关于角色，有个我个人的需求就是女友给我钱让我帮她点外卖，需要增加多角色系统吗？

我的需求：
工资，一个月4500，一个月，平均到每天。（但社保要扣去450的话就不这么算了），10号请假一天，这一天是0，15号迟到扣了20元，仍有不同，做减法或是记录。
固定的会员，月，年，这个也退不了，周期固定。
电动牙刷150元，15天前买的，用到现在，每天使用消耗是10元。等到30天时，平均每天就是5月，即平均使用周期。
一日三餐，一条记录很传统。
一箱苹果，6个18元，周一吃一个，周二一个，周三吃俩，后面没吃烂了扔了，那周一应该是18/3，周二是18/3*2。计数，成本转移。//可能必须结束才会真正计入，不然就会一直处于浮动。
一大瓶可乐，喝了两天，第一天三分之一，第二天三分之二，这种平均化处理就可以吧。
但一桶蛋白粉，一三五七喝，二四六不喝，有周期
一桶燕麦，有时候早饭吃，有时候没吃，不固定，也不好计量。
买了一个steam游戏，爽完了两天不想玩了，然后就结束吗，万一以后又玩呢？
关于重量/数量，可以记录，如买菜，可以记录单价，用于统计
关于购买时间和开始使用时间？如无网购的键盘，下月1才到，中间的时间要也算入平摊吗
关于退货，我用来两天不好用退货了，那是先减后加归零？
关于分类/标签，我想统计饮食，也想统计具体早中晚，还想统计具体吃了什么（苹果鸡蛋）
更夸张我想统计我一年吃了多少个鸡蛋，包括自己买的，外卖了包含的。
关于评价备注，例如饮食口味，产品优劣，个人体验，购买连接，店铺，平台，等也可以备注记录，下次购买时可以看看历史教训

我自己做的时候由mysql转为了mongodb，因为我看很多字段都可能是缺省值。这个需要怎么设置与考虑？
但还是使用mysql吧，缺省值使用null？还是使用json结构？

另外一个伟大的想法就是用户自定义编程统计，用户可以自己设置某标签之类的设置某种字段，进行某种合适的统计，可能需要某些灵活的字段。

暂时想到就这么多了

"手动打卡"类摊销（燕麦、可乐、蛋白粉、请假）

账户设置，如支付宝/微信/银行卡/家人账户/共同账户/女朋友账户？



数据库设计：
user：用户表，id，username，psaaword
account，账户表，userid，type,balance
category

多账户设计，如微信/支付宝/银行卡，主要是用来提现余额的吗？他有什么更丰富的作用，
如微信/支付宝/银行卡，我可能并不需要进行区分，在我看来是一种不必要的麻烦？我更喜欢这些都是我的钱，只是存放地方不一样，本质上都还是我的。
我更倾向与是我有一部分女朋友的资金，她给我的暂存，本质上是她的消费记录，我来帮她消费，这种账户才是她的。
也不排除用户可以自行添加创建，如主账户，育儿账户，彩礼金。一个账本用来负责某一类事件的。

关于分类，有时不想填的很详细我就选二级分类，有时想很详细，干到四级分类，如饮食/水果/苹果/烟台苹果。//这还得是用户自定义的吗还是全局定义的？还是用户自定义共享到全局？
分类和标签可以重复的吧，标签像是一种扁平化的可以多个，分类时树状的只有一个？

想统计"一年吃了多少鸡蛋"？	每次记外卖/买菜时，顺手勾一个 #鸡蛋 标签。年底统计这个标签即可
我是购买时记录标签呢，还是消耗时记录标签呢？
外卖的话，可以记录一个/半个
自己买了一袋鸡蛋，用了一个才记录一个才对？
消耗的时候是否也要备注呢？例如我喝蛋白粉意味着这一天可能锻炼了，备注一下锻炼内容？可能没啥必要有的偏了这个功能.但如买了一份大虾，第一天吃的水煮，第二天吃的油炸，可以记录作区分


1.默认一个主账户、需要时再细分
2.要我准备一份默认分类模板，暂时不需要很详细，虽然写在数据库了，但同时也写在程序里静态？。我想的是用户新建的分类可以被共享，但是需要一份用户个人喜好的json存储，记录他的创建，记录他用过的分类，不需要在分类表里记录是谁创建的。减少关联？我不确定我只是突然这样想。
3.嗯，消耗时也会有默认标签？创建时展示，但是感觉会麻烦？但有人愿意写有人不愿意写都可以。


一个问题时，切换到权责视图的话，一日三餐/转账押金这种是否也要显示呢？虽然没有权责记录，当作一次性记录进行展示？

主账号,默认就是个人所有资产，包括微信支付宝银行卡之类的，用户可以自定义拆分。可替代其他用户，如儿女压岁钱账户，如攒钱账户，如女友代付账户
借出/借入/押金/账户转账这种不需要权责视图记录，但切换到权责视图下时也应该显示展示？

这俩i段sh
owner_user_id	BIGINT UNSIGNED	否	消费归属人（MVP = user_id，预留给代付）
payer_user_id	BIGINT UNSIGNED	否	实际出钱人（MVP = user_id）

账户：主账户，余额：9999

购买午饭,鸭腿饭,
交易类型：支出
分类是：饮食/午饭
标签：鸭腿饭，鸡蛋

用户表。
username	VARCHAR(64)	否	唯一；登录名
password_hash	VARCHAR(128)	否	bcrypt
nickname	VARCHAR(64)	是	显示名
avatar	VARCHAR(255)	是	头像 URL

账户表。
user_id	BIGINT UNSIGNED	否	所属用户
name	VARCHAR(64)	否	显示名：如"招商银行"、"女友的钱"
type	VARCHAR(16)	否	见枚举 AccountType //我感觉这个没必要，是用户自定义才对，也就是name。
balance	DECIMAL(14,2)	否	当前余额（由应用层维护；冷备可 rebuild） //余额这里怎么设计的，是固定值？还是初始值？统计值？随时可修改？
currency	CHAR(3)	否	默认 CNY
is_virtual	TINYINT(1)	否	1=虚拟账户（代付：女友/家人/共同） //这个也不需要吧，用account进行区分，都是真实的？
is_archived	TINYINT(1)	否	1=已归档，不再出现在录入默认列表 //已归档是删除吗？什么情况下归档，是隐藏？归当前要把记录转移吗
sort	INT	否	排序
note	VARCHAR(255)	是	备注


分类表
3.3 category — 分类（树形）
字段	类型	允许空	说明
user_id	BIGINT UNSIGNED	否	//没必要？
parent_id	BIGINT UNSIGNED	是	NULL 表示一级分类
name	VARCHAR(64)	否	
kind	VARCHAR(16)	否	INCOME / EXPENSE / TRANSFER / OTHER
icon	VARCHAR(64)	是	图标名
color	VARCHAR(16)	是	#RRGGBB //暂时不考虑颜色删掉
sort	INT	否	

//我是想设计为系统默认值，用户上传共享但是不归用户，存储用户上传/使用记录配置（全部存储）
//但用户新增上传，重复怎样？图标重复怎样？我想可以先设置为固定选择，让他自己设置标签？
//我想还不如我提供丰富的选择固定化值，用户选择用就行了，先不让用户自定义？
//还是user_id=1为系统？
3.4 tag — 标签
字段	类型	允许空	说明
user_id	BIGINT UNSIGNED	否	
name	VARCHAR(64)	否	
color	VARCHAR(16)	是	//暂时不考虑颜色删掉
//tag也是，必须要强绑定用户id吗？我想用户如果修改的话就是重新选择建一个，不然太重复了，每个人都创建同样的标签？

3.5 transaction_tag — 交易-标签多对多（购买视角）
字段	类型	允许空	说明
transaction_id	BIGINT UNSIGNED	否	
tag_id	BIGINT UNSIGNED	否	
//这个是购买时标签，就是创建时添加？

accrual_entry_tag — 权责-标签多对多（消耗视角）
字段	类型	允许空	说明
accrual_entry_id	BIGINT UNSIGNED	否	
tag_id	BIGINT UNSIGNED	否	
//消耗时打标签，也就是打卡记录，这里什么消耗都会记录吗？好像不太好。莫名感觉这种应该写在json里？"一年吃了多少鸡蛋"这种消耗量统计。json中还可以记录数量？


3.6 resource — 可摊销资源定义
何时建 resource？ 需要跨多天/多次消耗、或需要按次数/周期摊销的物品才建。一日三餐这种"买完即吃完"无需建。

字段	类型	允许空	说明
user_id	BIGINT UNSIGNED	否	
name	VARCHAR(128)	否	电动牙刷 / 一箱苹果 / 年会员
category_id	BIGINT UNSIGNED	是	
unit	VARCHAR(16)	是	"天" / "个" / "次" / "ml"...
total_qty	DECIMAL(14,4)	是	总量（6 个、30 天、null）
remaining_qty	DECIMAL(14,4)	是	剩余量（应用层维护）
total_cost	DECIMAL(14,2)	否	总成本（= 购买金额）
amortized_cost	DECIMAL(14,2)	否	已摊销累计（冗余，便于快速查询）
amortize_rule	JSON	否	摊销规则，见 §4.2
status	VARCHAR(16)	否	见 ResourceStatus
purchase_at	DATETIME	否	购买时间（出现金流）
start_use_at	DATETIME	是	开始使用时间（摊销起点）//默认购买时间？
end_at	DATETIME	是	结束时间（status 改变时写入）
note	VARCHAR(512)	是	
ext	JSON	是	用户自定义字段（单价、店铺、购买链接、评价...）
ResourceStatus 枚举：ACTIVE（使用中） / PAUSED（暂停：如 Steam 游戏吃灰）/ ENDED（正常结束）/ RETURNED（退货）/ DISCARDED（丢弃/烂了）

索引：(user_id, status, start_use_at)；生成列 rule_type 来自 amortize_rule->'$.type' 并加索引，用于"查询所有按天摊销的资源"。

3.7 transaction — 交易主单
一次业务发生的"单据"，派生 0~N 条 cash_entry 和 0~N 条 accrual_entry。

字段	类型	允许空	说明
user_id	BIGINT UNSIGNED	否	
owner_user_id	BIGINT UNSIGNED	否	消费归属人（MVP = user_id，预留给代付） //不需要
payer_user_id	BIGINT UNSIGNED	否	实际出钱人（MVP = user_id） //不需要
type	VARCHAR(16)	否	见 TransactionType
occur_at	DATETIME	否	业务发生时间
amount	DECIMAL(14,2)	否	金额（总额，INCOME/EXPENSE 为正，内部符号由 type 决定）
account_id	BIGINT UNSIGNED	是	主账户（TRANSFER 的出方）
to_account_id	BIGINT UNSIGNED	是	TRANSFER 的入方 //这个需要吗，可以为空是的。
category_id	BIGINT UNSIGNED	是	
resource_id	BIGINT UNSIGNED	是	关联的资源（如买电动牙刷）
title	VARCHAR(128)	是	一句话描述
note	VARCHAR(1024)	是	
ext	JSON	是	自定义字段：店铺、平台、评价、购买链接...


3.8 cash_entry — 现金流条目（钱何时动）
字段	类型	允许空	说明
user_id	BIGINT UNSIGNED	否	
transaction_id	BIGINT UNSIGNED	否	
account_id	BIGINT UNSIGNED	否	
direction	VARCHAR(4)	否	IN / OUT
amount	DECIMAL(14,2)	否	正数
happen_at	DATETIME	否	现金流发生时间（= transaction.occur_at，或延后如信用卡还款）
note	VARCHAR(255)	是	

//这个不要明白，这不重复吗，记录不已经都有了吗？


3.9 accrual_entry — 权责条目（价值何时耗）
字段	类型	允许空	说明
user_id	BIGINT UNSIGNED	否	
transaction_id	BIGINT UNSIGNED	否	
resource_id	BIGINT UNSIGNED	是	关联资源；一次性消费（吃饭）为 NULL
category_id	BIGINT UNSIGNED	是	
amount	DECIMAL(14,2)	否	金额（正数为消耗；退货/校正可为负）
qty	DECIMAL(14,4)	是	消耗数量（1 个、0.5 瓶）
unit	VARCHAR(16)	是	
accrue_at	DATETIME	否	权责发生时间（当天/该次）
source	VARCHAR(16)	否	AUTO（规则自动生成）/MANUAL（手动打卡）/END_SETTLE（结束结算一次性摊入）/ADJUST
note	VARCHAR(255)	是	
索引：(user_id, accrue_at)、(resource_id, accrue_at)、(transaction_id)、(category_id, accrue_at)
//这个时权责明细。

6. GORM 模型骨架（参考）
// service/model/common.go
type Base struct {
    ID        uint64         `gorm:"primaryKey" json:"id"`
    CreatedAt time.Time      `json:"created_at"`
    UpdatedAt time.Time      `json:"updated_at"`
    DeletedAt gorm.DeletedAt `gorm:"index" json:"-"`
}
这个用grom.Model就行吧